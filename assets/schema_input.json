{
  "$schema": "http://json-schema.org/draft-07/schema",
  "$id": "https://raw.githubusercontent.com/nf-austin/copykat/main/assets/schema_input.json",
  "title": "nf-austin/copykat samplesheet",
  "description": "One row per sample: an AnnData .h5ad file to run CopyKAT on.",
  "type": "array",
  "items": {
    "type": "object",
    "properties": {
      "sample": {
        "type": "string",
        "pattern": "^\\S+$",
        "errorMessage": "Sample name must be provided and must not contain spaces",
        "meta": ["id"],
        "description": "Sample identifier. Becomes the output subdirectory name, so it must be unique."
      },
      "h5ad": {
        "type": "string",
        "format": "file-path",
        "exists": true,
        "pattern": "^\\S+\\.h5ad$",
        "errorMessage": "h5ad path must be provided, must not contain spaces, and must end in .h5ad",
        "description": "Path to the AnnData .h5ad file holding the raw count matrix."
      }
    },
    "required": ["sample", "h5ad"]
  }
}
